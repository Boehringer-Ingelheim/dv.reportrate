% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_metric_calc.R
\name{merge_event_data_with_grouping}
\alias{merge_event_data_with_grouping}
\title{Internal helper function for combining the preprocessed disposition (DS) and adverse event (AE) datasets into one
unified event stream. If a valid grouping variable is provided, the function enriches the combined dataset with the
corresponding group assignment from the demographics (DM) domain.}
\usage{
merge_event_data_with_grouping(
  dm_dataset,
  ds_prep_dataset,
  ae_prep_dataset,
  subjid_var,
  grouping_var
)
}
\arguments{
\item{dm_dataset}{\verb{[data.frame]} The demographics dataframe.}

\item{ds_prep_dataset}{\verb{[data.frame]} The disposition dataframe returned by the prepare_ds_data() function.}

\item{ae_prep_dataset}{\verb{[data.frame]} The adverse event dataframe returned by the prepare_ae_data() function.}

\item{subjid_var}{\verb{[character(1)]} Character name of the unique subject identifier column in all dataframes.}

\item{grouping_var}{\verb{[character(1)]} Character name of the grouping variable (column) in the "dm_dataset". The
values of this column define the different groups in the plot.}
}
\value{
A dataframe containing all DS and AE events row‑bound into a unified event dataset. The result always
includes the harmonised columns required for the downstream computation of both metrics. If a valid grouping_var is
provided, an additional column with the group assignment is attached to each event record via a left join.
}
\description{
Internal helper function for combining the preprocessed disposition (DS) and adverse event (AE) datasets into one
unified event stream. If a valid grouping variable is provided, the function enriches the combined dataset with the
corresponding group assignment from the demographics (DM) domain.
}
\keyword{internal}
